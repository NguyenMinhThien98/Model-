import {StyleSheet} from 'react-native';
import {Colors} from '../../assets/colors';

const styles = StyleSheet.create({
  yAxisText: {color: Colors.white},
  lableBottom: {width: 20},
});
export default styles;
